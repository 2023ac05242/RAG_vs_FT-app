# --- Core numeric stack
numpy==2.0.2
pandas==2.2.2
scikit-learn==1.4.2   # app uses TfidfVectorizer & cosine_similarity

# --- PyTorch (CPU)
# No "+cpu" suffix — let pip pick the CPU wheels from PyPI
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# --- NLP + retrieval
transformers==4.44.2
tokenizers==0.19.1
sentence-transformers==2.7.0
faiss-cpu==1.8.0.post1

# --- App
streamlit==1.48.1
